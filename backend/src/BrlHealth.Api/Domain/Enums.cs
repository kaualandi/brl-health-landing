namespace BrlHealth.Api.Domain;

public enum Sex { Male, Female }

public enum ActivityLevel { Sedentary, Light, Moderate, Active, Athlete }

public enum Goal { Lose, Recomp, Gain, Performance, Health }

public enum PlanId { Free, Pro, Family }
